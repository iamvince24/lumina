import { motion } from 'framer-motion';

/**
 * Framer Motion SVG primitives for animating mind map elements
 * These components allow us to animate SVG elements declaratively
 */

export const MotionGroup = motion.g;
export const MotionRect = motion.rect;
export const MotionPath = motion.path;
export const MotionText = motion.text;
export const MotionCircle = motion.circle;
export const MotionLine = motion.line;
